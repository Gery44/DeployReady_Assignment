# Copy this file to .env and fill in the values before running docker compose.
# The real .env must never be committed to source control.

# Port the application listens on inside the container.
PORT=3000
